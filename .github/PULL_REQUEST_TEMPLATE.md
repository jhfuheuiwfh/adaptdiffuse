## What changed

<!-- Short description of the change and why it is needed. -->

## How to test

<!-- Steps to reproduce / verify. Example: run `launch.bat`, open http://127.0.0.1:7860, generate with backend `cpu`. -->

## Checklist

- [ ] I tested this on Windows with `launch.bat` (or noted why it was not possible)
- [ ] Backend cascade still works for the paths I touched (CUDA / Vulkan / ROCm / DirectML / CPU)
- [ ] I did not commit generated files (`backend.json`, `.setup_done`, `bin/`, `models/`, `outputs/`, `.venv/`)
- [ ] README updated if behaviour or setup changed
